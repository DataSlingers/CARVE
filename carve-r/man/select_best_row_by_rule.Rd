% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection.R
\name{select_best_row_by_rule}
\alias{select_best_row_by_rule}
\title{Select the best row of a CARVE results table by a selection rule}
\usage{
select_best_row_by_rule(
  results_df,
  measure,
  rule = c("max", "1se", "quantile"),
  return_idx = FALSE,
  not_two = FALSE
)
}
\arguments{
\item{results_df}{A data frame or tibble produced by CARVE's `fit()`. Must
have a `n_clusters` column and the metric column corresponding to
`measure` (plus `_se` / `_upper` / `_lower` columns for 1se / quantile).}

\item{measure}{Metric key. See [MEASURE_MAP] for accepted aliases.}

\item{rule}{One of `"max"`, `"1se"`, or `"quantile"`.}

\item{return_idx}{Logical; if `TRUE` return the selected row's integer
position instead of the row itself.}

\item{not_two}{Logical; if `TRUE`, rows with `n_clusters == 2` are excluded
from selection.}
}
\value{
A one-row data frame (or the integer position if `return_idx`).
}
\description{
Mirrors `carve._selection.select_best_row_by_rule`. Dispatches to
[select_best_row_max()], [select_best_row_1se()], or
[select_best_row_quantile()].
}

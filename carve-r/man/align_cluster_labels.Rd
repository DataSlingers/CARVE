% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{align_cluster_labels}
\alias{align_cluster_labels}
\title{Align cluster labels to a reference via Hungarian assignment}
\usage{
align_cluster_labels(reference_labels, labels)
}
\arguments{
\item{reference_labels}{Integer vector of reference labels.}

\item{labels}{Integer vector of labels to align.}
}
\value{
Integer vector of aligned labels with the same length as `labels`.
}
\description{
Mirrors `_utils.align_cluster_labels`. Builds a contingency matrix, solves
the assignment problem with [clue::solve_LSAP()] to maximize agreement,
and returns `labels` with the permutation applied. Predicted classes
that were not matched (when the predicted label space is larger than the
reference) are left unchanged.
}

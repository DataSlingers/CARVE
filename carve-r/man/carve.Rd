% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carve_generic.R
\name{carve}
\alias{carve}
\alias{carve.default}
\alias{carve.matrix}
\alias{carve.data.frame}
\alias{carve.Seurat}
\alias{carve.SingleCellExperiment}
\title{Fit a CARVE validator}
\usage{
carve(x, ...)

\method{carve}{default}(
  x,
  n_clusters = 2:10,
  n_resamples = 100L,
  subsample_ratio = 0.8,
  estimator_param_grids = "light",
  normalization_options = NULL,
  dim_reduction_options = NULL,
  classifier = NULL,
  n_trees = 100L,
  reference_labels = NULL,
  n_jobs = 1L,
  random_state = NULL,
  verbose = 0L,
  reference_labels_fit = NULL,
  randomize_preprocessing = FALSE,
  show_progress = FALSE,
  mode = "default",
  fit_random_state = NULL,
  ...
)

\method{carve}{matrix}(
  x,
  n_clusters = 2:10,
  n_resamples = 100L,
  subsample_ratio = 0.8,
  estimator_param_grids = "light",
  normalization_options = NULL,
  dim_reduction_options = NULL,
  classifier = NULL,
  n_trees = 100L,
  reference_labels = NULL,
  n_jobs = 1L,
  random_state = NULL,
  verbose = 0L,
  reference_labels_fit = NULL,
  randomize_preprocessing = FALSE,
  show_progress = FALSE,
  mode = "default",
  fit_random_state = NULL,
  ...
)

\method{carve}{data.frame}(x, ...)

\method{carve}{Seurat}(
  x,
  assay = "RNA",
  slot = "data",
  features = NULL,
  reduction = NULL,
  n_dims = NULL,
  ...
)

\method{carve}{SingleCellExperiment}(
  x,
  assay = "logcounts",
  reduction = NULL,
  n_dims = NULL,
  features = NULL,
  ...
)
}
\arguments{
\item{x}{A numeric matrix, data frame, `Seurat` object, or
`SingleCellExperiment` object.}

\item{...}{Passed to the method.}

\item{n_clusters, n_resamples, subsample_ratio, estimator_param_grids, normalization_options, dim_reduction_options, classifier, n_trees, reference_labels, n_jobs, random_state, verbose}{Forwarded to [`CARVE$new()`][CARVE].}

\item{reference_labels_fit}{Optional reference labels passed to `$fit()`.}

\item{randomize_preprocessing, show_progress, mode, fit_random_state}{Forwarded to [`$fit()`][CARVE].}

\item{assay}{For `Seurat` / `SingleCellExperiment` inputs, the assay
to extract (default `"RNA"` for Seurat, `"logcounts"` for SCE).}

\item{slot}{For `Seurat` inputs, the slot/layer (default `"data"`).
Gated on `packageVersion("Seurat")` so both v4 (`slot`) and v5
(`layer`) APIs work.}

\item{features}{Optional character vector of features/rows to use.}

\item{reduction}{Optional reduction name (e.g. `"pca"`); when set,
the embedding matrix is used instead of the assay data.}

\item{n_dims}{Optional integer; number of leading components of the
reduction to keep.}
}
\value{
A fitted [CARVE] instance.
}
\description{
Convenience wrapper around `CARVE$new(...)$fit(x, ...)`. Dispatches
on the class of `x` so `carve()` works uniformly on matrices, data
frames, `Seurat` objects, and `SingleCellExperiment` objects.
}
\examples{
set.seed(1)
X <- rbind(
  matrix(rnorm(60, 0, 0.4), ncol = 2),
  matrix(rnorm(60, 5, 0.4), ncol = 2)
)
fit <- carve(X, n_clusters = 2:3, n_resamples = 5,
             estimator_param_grids = list(
               list(type = "kmeans", grid = list(n_clusters = 2:3))
             ),
             random_state = 1L)
fit$get_k(measure = "stability", rule = "max")

}

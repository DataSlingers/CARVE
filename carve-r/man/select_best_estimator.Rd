% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/selection.R
\name{select_best_estimator}
\alias{select_best_estimator}
\title{Select the best estimator configuration as a list}
\usage{
select_best_estimator(
  results_df,
  measure = "stability",
  rule = c("max", "1se", "quantile"),
  k = NULL,
  not_two = FALSE
)
}
\arguments{
\item{results_df}{A data frame or tibble produced by CARVE's `fit()`. Must
have a `n_clusters` column and the metric column corresponding to
`measure` (plus `_se` / `_upper` / `_lower` columns for 1se / quantile).}

\item{measure}{Metric key. See [MEASURE_MAP] for accepted aliases.}

\item{rule}{One of `"max"`, `"1se"`, or `"quantile"`.}

\item{k}{Optional integer. If supplied, filter `results_df` to that
`n_clusters` before selection.}

\item{not_two}{Logical; if `TRUE`, rows with `n_clusters == 2` are excluded
from selection.}
}
\value{
A list with components `estimator` (character) and `params` (named
  list of hyperparameters).
}
\description{
Returns the selected row's estimator name and parameter set. Estimator
instantiation (mirroring Python's `build_estimator_from_row`) is deferred
to the R6 `CARVE$get_estimator()` method in later phases.
}

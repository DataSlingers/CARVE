% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus.R
\name{compute_consensus_matrix}
\alias{compute_consensus_matrix}
\title{Compute a consensus matrix from resampled clustering runs}
\usage{
compute_consensus_matrix(n_samples, runs, return_counts = FALSE)
}
\arguments{
\item{n_samples}{Integer, total number of samples in the original dataset.}

\item{runs}{A list of runs, each a two-element list with components
`sample_idx` (1-based integer vector of indices into the original
dataset) and `labels` (cluster labels for those indices).}

\item{return_counts}{Logical; if `TRUE`, also return the raw co-cluster
and co-sample count matrices.}
}
\value{
If `return_counts = FALSE`, an `(n_samples, n_samples)` numeric
  matrix. Otherwise a list with components `consensus_matrix`,
  `co_cluster_counts`, and `co_sample_counts`.
}
\description{
For each pair of samples `(i, j)`, the consensus value is the number of
runs in which `i` and `j` were assigned to the same cluster divided by the
number of runs in which both `i` and `j` appeared in the subsample. Pairs
that never co-occurred in any subsample are `NaN`.
}

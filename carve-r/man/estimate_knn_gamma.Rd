% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grids.R
\name{estimate_knn_gamma}
\alias{estimate_knn_gamma}
\title{Estimate RBF gamma values from a k-NN median heuristic}
\usage{
estimate_knn_gamma(X, n_neighbors = 7L, multipliers = c(0.5, 1, 2))
}
\arguments{
\item{X}{Numeric matrix of shape `(n_samples, n_features)`.}

\item{n_neighbors}{Integer; number of neighbors (default `7`).}

\item{multipliers}{Numeric vector of scale multipliers applied to sigma.}
}
\value{
Numeric vector of gamma values.
}
\description{
Mirrors `_grids.estimate_knn_gamma`. Computes the median k-th neighbor
distance and returns `gamma = 1 / (2 * (m * sigma)^2)` for each `m` in
`multipliers`.
}
